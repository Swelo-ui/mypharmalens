# Supabase Configuration (already configured in client.ts)
# SUPABASE_URL=https://vcshydrusnuxsxwctnod.supabase.co
# SUPABASE_ANON_KEY=your_anon_key_here

# Required for Supabase Edge Functions (optional for main app)
# GEMINI_API_KEY=your_gemini_api_key_here

# Note: The main application uses hardcoded Supabase credentials in src/integrations/supabase/client.ts
# Environment variables are only needed if you plan to deploy or modify the edge functions